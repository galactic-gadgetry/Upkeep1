<UserControl x:Class="Upkeep1.UIComponents.Buttons.NavigationBarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Upkeep1.UIComponents.Buttons"
             mc:Ignorable="d" 
             d:DesignWidth="250">
    
    <Grid>
        <Border>

            <Button
                Command="{Binding ButtonClickedCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}">
                <StackPanel Orientation="Horizontal">
                    <Image>
                        
                    </Image>
                    <TextBlock Text="{Binding ButtonText, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}">
                        
                    </TextBlock>
                </StackPanel>
            </Button>
            
        </Border>
    </Grid>
    
</UserControl>
