<Window x:Class="Upkeep1.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Upkeep1.Views"
        xmlns:ui="clr-namespace:Upkeep1.UIComponents"
        xmlns:vm="clr-namespace:Upkeep1.ViewModels"
        mc:Ignorable="d"
        MinHeight="510" MinWidth="950"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Grid>

        <ContentControl Content="{Binding CurrentMainContentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:LayoutViewModel}">
                    <ui:Layout/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
        
    </Grid>
    
</Window>
